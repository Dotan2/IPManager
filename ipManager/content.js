// Content script for Lab IP Manager
// PASSIVE MODE - No UI interference, only provides context menu support

(function() {
  'use strict';

  // This content script is now completely passive
  // It does NOT modify any page content, styles, or behavior
  // It only provides context menu support for selected text
  
  console.log('Lab IP Manager: Content script loaded (passive mode)');
  
  // No DOM manipulation, no styling, no highlighting
  // The extension works purely through:
  // 1. Context menu (right-click on selected text)
  // 2. Popup interface
  // 3. Omnibox search
  
  // This ensures zero interference with web page functionality

})();
